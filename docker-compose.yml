version: '3'

services:

  # Application container
  app:
    image: node:12-alpine
    container_name: app
    env_file:
      - ./.env
    volumes:
      - ./:/app
    working_dir: /app
    ports:
      - 8080:8080
    command: "npm run dev"
